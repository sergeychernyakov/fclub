// Assuming dashboard.js needs similar functions, they can be included here.

document.addEventListener('DOMContentLoaded', () => {
    fetchGamesList();
});

// Reuse functions from users.js if needed or create specific ones for dashboard.js
